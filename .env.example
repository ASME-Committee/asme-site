# ASME Supabase (server-side only — used by /api/join to save membership applications)
# Get these from the new ASME Supabase project: Settings -> API
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Existing: used by /api/articles/generate (leave as-is if already set)
# ANTHROPIC_API_KEY=
